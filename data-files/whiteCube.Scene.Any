// -*- c++ -*-

{
	name = "White Cube";

	models = {
		cubeModel = ArticulatedModel::Specification {
			filename = "model/crate/crate.obj";
			preprocess = {
				setMaterial(all(), Color3(1, 1, 1));
			};
		};
	};

	entities = {
		skybox = Skybox {
			texture = "cubemap/whiteroom/whiteroom-*.png";
		};


		cube0 = VisibleEntity{
			model = "cubeModel";
			frame = CFrame::fromXYZYPRDegrees(0, 0, -2, 45, 0, 0);	// Away from the camera starting pos, 45 rotated in vertical axis
		};

		camera = Camera{
			frame = CFrame::fromXYZYPRDegrees(0,0,5);
		};

	};

};